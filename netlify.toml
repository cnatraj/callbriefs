# Netlify config — Vite SPA build + client-side routing fallback.

[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every unknown path serves index.html so Vue Router handles it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
